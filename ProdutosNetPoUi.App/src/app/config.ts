// src/config.ts

export const CONFIG = {
  // 🚨 Mude para a URL INTERNA do Docker (http://api:8080/api/v1)
  // antes de rodar o comando 'docker-compose up --build'
  apiUrl: 'http://localhost:5000/api/v1' // Endereço de desenvolvimento
};